version: "3.8"

services:
  open-terminal:
    build:
      context: .
      dockerfile: Dockerfile.slim
    container_name: open-terminal
    restart: unless-stopped
    ports:
      - "8017:8000"
    env_file:
      - .env
